{
  "name": "@executor-js/e2e-mcp-apps",
  "private": true,
  "description": "Render + test executor's generative-UI MCP Apps in a real MCP-Apps host (sunpeak), headless, no VM.",
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/check-sunpeak.mjs",
    "pretest": "bun run --cwd ../../packages/hosts/mcp-apps-shell build:shell",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "sunpeak": "^0.20.82"
  }
}
